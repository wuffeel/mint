import 'package:flutter/material.dart';

class ReviewSliverSection extends StatelessWidget {
  const ReviewSliverSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
